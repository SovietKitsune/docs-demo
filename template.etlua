<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
        integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"
        integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj"
        crossorigin="anonymous"></script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhRq26LrZ/JpcUGGOn+Y7RsweNrtN/tE3MoK7ZeZDyx"
        crossorigin="anonymous"></script>

    <script src="https://kit.fontawesome.com/04ffc8e1e8.js" crossorigin="anonymous"></script>

    <link rel="stylesheet"
        href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/10.4.1/styles/atom-one-light.min.css">

    <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/10.4.1/highlight.min.js"></script>

    <script differ>hljs.initHighlightingOnLoad();</script>

    <style>
        html, body {
            margin: 0;
            height: 100%;
        }

        .container-fluid, #main-content, .sidebar {
            height: 100%;
        }
    </style>

    <title>Client</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a href="/" class="navbar-brand"><%= project.name %></a>

        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">Home</a>
                </li>

                <!-- TODO: Give active to whatever page we are currently generating -->

                <!--
                <li class="nav-item active">
                    <a href="/docs.html" class="nav-link">Documentation</a>
                </li>
                -->
            </ul>

            <!-- TODO: Don't insert bottom part in if no source is specified -->
            <% if project.source then %>
                <ul class="navbar-nav align-self-end">
                    <li class="nav-item">
                        <!-- TODO: Replace with whatever source we get in the config -->
                        <a href="<%= project.source %>" class="nav-link">
                            <i class="fab fa-git-alt"></i> Source
                        </a>
                    </li>
                </ul>
            <% end %>
        </div>
    </nav>

    <div class="container">
        <div class="row" id="main-content">
            <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse bg-light">
                <div class="sidebar-sticky pt-3">
                    <!-- TODO: Skip loading topics if no markdown has been added -->
                    <% if #project.topics > 0 then %>
                        <h5 class="sidebar-heading">
                            <span>Topics</span>
                        </h5>

                        <ul class="nav flex-column">
                            <% for _, v in pairs(project.topics) do %>
                                <li class="nav-item">
                                    <!-- TODO: Render topics -->
                                    <a href="<%= v.location %>" class="nav-link"><%= v.name %></a>
                                </li>
                            <% end %>
                        </ul>
                    <% end %>

                    <!-- TODO: Skip loading if there are no classes 
                        If there are classes, make sure non are marked as structures
                    -->

                    <% if #project.classes > 0 then%>
                        <h5 class="sidebar-heading">
                            <span>Classes</span>
                        </h5>

                        <ul class="nav flex-column">
                            <% for _, v in pairs(project.classes) do %>
                                <li class="nav-item">
                                    <a href="<%= v.location %>" class="nav-link <%= (v.name == current.name and current.type == 'class' and 'disabled') or '' %>">
                                        <%= v.name %>
                                    </a>
                                </li>
                            <% end %>
                            
                            <!-- TODO: Disabled the currently selected page/current page being rendered -->
                            <!--
                            <li class="nav-item">
                                <a href="#" class="nav-link disabled">Embed</a>
                            </li>
                            -->
                        </ul>
                    <% end %>

                    <!-- TODO: Skip loading if there are no modules -->

                    <% if #project.modules > 0 then%>
                        <h5 class="sidebar-heading">
                            <span>Modules</span>
                        </h5>

                        <ul class="nav flex-column">
                            <% for _, v in pairs(project.modules) do %>
                                <li class="nav-item">
                                    <a href="<%= v.location %>" class="nav-link <%= (v.name == current.name and current.type == 'module' and 'disabled') or '' %>">
                                        <%= v.name %>
                                    </a>
                                </li>
                            <% end %>
                        </ul>
                    <% end %>

                    <!-- TODO: Skip loading if there are no classes marked as structures/types -->

                    <% if #project.structures > 0 then%>
                        <h5 class="sidebar-heading">
                            <span>Structures</span>
                        </h5>

                        <ul class="nav flex-column">
                            <% for _, v in pairs(project.structures) do %>
                                <li class="nav-item">
                                    <a href="<%= v.location %>" class="nav-link <%= (v.name == current.name and current.type == 'structure' and 'disabled') or '' %>">
                                        <%= v.name %>
                                    </a>
                                </li>
                            <% end %>
                        </ul>
                    <% end %>
                </div>
            </nav>

            <main class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
                <!-- TODO: Replace with the actual content and use markdown to render most things -->

                <h1>
                    <%= current.type:sub(0, 1):upper() .. current.type:sub(2, #current.type) %> <code><%= current.name %></code>
                </h1>

                <a href="#" class="align-self-end">
                    <i class="fas fa-code"></i> View source
                </a>

                <hr>

                <!-- TODO: This description should be rendered with markdown just incase it uses it -->
                <p><%- current.desc or current.short_desc or 'none' %></p>

                <% 
                    local initFunc

                    for _, v in pairs(current.methods) do
                        if v.name == '__init' or v.name == 'init' or v.name == 'initialize' then
                            initFunc = v
                            break
                        end
                    end
                %>

                <% if current.type == 'class' then %>
                    <h3>Constructor</h3>

                    <!-- TODO: `toast.className(params)` -->
                    <pre><code class="lua"><%= project.name %>.<%= current.name %>(<%= initFunc and constructorParams(initFunc) or 'unknown' %>)</code></pre>
                <% elseif current.type == 'module' then %>
                    <h3>Import</h3>

                    <pre><code class="lua">local <%= current.name %> = <%= project.name %>.<%= current.name %></code></pre>
                <% end %>

                <!-- TODO: Optional would be if it has `| nil` as the doc generator can't seem to pickup on it 
                    If the type is a standard Lua type then link to the Lua documentation
                    If the type is a class or structure within the library, link to it
                    If the description is `""` then set it to `None`
                    The default would be the string literal types **if**  the parameter has `| nil` to support enums
                -->

                <% if current.type == 'class' then %>
                    <%- initFunc and genParams(initFunc) or '' %>
                <% end %>

                <!-- TODO: Only show properties and methods if they are not prefixed with `_` or marked as private -->

                <%
--[[%>
 <table class="table table-borderless text-secondary">
                    <thead>
                        <% if #current.fields > 0 then %>
                            <th scope="col">Properties</th>
                        <% end %>
                        <% if #current.methods > 0 then %>
                            <th scope="col">Methods</th>
                        <% end %>
                    </thead>
                    <tbody>
                        <!-- TODO: If there is more of one than other, leave the other blank -->
                        <%- paramTable(current) %>
                    </tbody>
                </table> 
<%
]]%>

                <% if #current.fields > 0 then %>
                    <h3>Properties</h3>
                    <% for _, v in pairs(current.fields) do%>
                        <span><small><%= current.name %>.</small><%= v.name %></span>: <code><%- typeResolver(v.type) %></code>
                        <% if (v.desc and v.desc ~= '') or (v.short_desc and v.short_desc ~= '') then %>
                            <blockquote>
                                <%- md((v.desc and v.desc ~= '' and v.desc) or (v.short_desc and v.short_desc ~= '' and v.short_desc)) %>
                            </blockquote>
                        <% end %>
                        <br>
                    <% end %>
                <% end %>

                <% if #current.methods > 0 then %>
                    <br>

                    <h3>Methods</h3>
                    <% for _, v in pairs(current.methods) do%>
                        <h5><small><%= current.name %><%= v.is_static and '.' or ':' %></small><%= v.name %>(<%= constructorParams(v) %>) -> <%= #v.returns == 0 and 'nil' or '' %> 
                        
                        <% for _, ret in pairs(v.returns) do %>
                            <%= typeResolver(ret.type) %>
                        <% end %>
                        </h5>
                        <% if (v.desc and v.desc ~= '') or (v.short_desc and v.short_desc ~= '') then %>
                            <blockquote>
                                <%- md((v.desc and v.desc ~= '' and v.desc) or (v.short_desc and v.short_desc ~= '' and v.short_desc)) %>

                                <%- genParams(v) %>
                            </blockquote>
                        <% end %>
                        <br>
                    <% end %>
                <% end %>
            </main>
        </div>
    </div>
</body>

</html>